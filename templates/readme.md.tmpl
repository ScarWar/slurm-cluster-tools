# {{PROJECT_NAME}}

SLURM project generated by [slx](https://github.com/slurm-cluster-tools).

## Quickstart

1. Edit your run script: `{{RUN_NAME}}.sh`
2. Submit the job:
   ```bash
   slx project submit {{PROJECT_NAME}}
   # or from the project directory:
   sbatch {{SBATCH_NAME}}.sbatch
   ```

## Logs

Job outputs are saved to the `logs/` directory:
- `logs/{{PROJECT_NAME}}_<job_id>.out` - stdout
- `logs/{{PROJECT_NAME}}_<job_id>.err` - stderr

Monitor a running job with:
```bash
slx tail <job_id>
```

